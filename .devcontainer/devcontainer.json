{
  "name": "comdata",
  "image": "mcr.microsoft.com/devcontainers/python:3.11-bookworm",
  "features": {
      "ghcr.io/devcontainers/features/docker-in-docker:1": {
          "version": "latest"
      },
      // This is the best way to get Node & npm installed automatically
      "ghcr.io/devcontainers/features/node:1": {
          "version": "20" 
      }
  },
  "customizations": {
      "vscode": {
          "extensions": [
              "ms-python.python",
              "cweijan.vscode-database-client2",
              "google.gemini-code-assist"
          ]
      }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y sqlite3 default-mysql-client && pip3 install -r requirements.txt && npm install -g @google/gemini-cli @openai/codex && curl -fsSL https://claude.ai/install.sh | bash && pip3 install aider-install && curl -Ls https://cli.doppler.com/install.sh | sudo bash",
  // "postStartCommand": "bash -c 'doppler secrets download --no-file --format env > .env'"
}
